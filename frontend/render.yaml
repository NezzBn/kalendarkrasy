services:
  - type: web
    name: kalendarkrasy-frontend
    env: static
    buildCommand: yarn install && yarn build
    staticPublishPath: ./build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
